version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15
    steps:
      - checkout
      - run: echo build
  test:
    docker:
      - image: circleci/node:10.15
    steps:
      - checkout
      - run: echo test
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test